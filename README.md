# 🎓 TypeScript Student Table

This project is a simple **TypeScript + Webpack** application that demonstrates the use of interfaces, static typing, and DOM manipulation with vanilla JavaScript.

It creates a list of students and dynamically renders their **first names** and **locations** inside an HTML table.

---

## 🧠 Features
- Written entirely in **TypeScript**
- Uses a **Student interface** with typed fields
- Demonstrates **array iteration** and **DOM rendering**
- Bundled with **Webpack**
- Passes all type checks (`No type errors found`)

---

## 📂 Project Structure
